#include "minishell.h"

command
argument
chemin

void	is_operator()
{
	if (cmd[i] == ' ' || cmd[i] == '\t')
		i++;
	else if (cmd[i] == '|')
		return ();
	else if ()
}

char	**tokenizer(char *cmd, t_struct)
{
}
