#include "so_long.h"

void	verif_tab(char **tab)
{
	int	*_0;
	int	*_1;
	int	*_C;
	int	*_E;
	int	*_P;

	while (tab[y])
	{
		if (!size_line())
			return (0);
		if (!verif_elemet())

	}
}

void	parsing_map()
{
	init_tab()
	while (tab[y])
	{
		if (!verif_line(tab[y]))
			error;
		y++;
	}
}
