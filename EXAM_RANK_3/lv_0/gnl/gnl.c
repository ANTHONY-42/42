#include <unistd.h>

void	get_next_line()
{
	static char	buffer[BUFFER_SIZE];
	char		line[70000]
	static int	pos;
	static int	read;
	
	while (1)
	{
		if (pos >= nb_read)
		{
			buffer_read = reaad()
		}
		if (line[i] == '\n')
			break;
		line[i] = buffer[buffer_pos++];
	}
}

int main()
{
}
